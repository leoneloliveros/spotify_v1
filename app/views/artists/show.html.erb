<h1><%= @artist.name %></h1>
<img src="<%= @artist.image_url %>" alt="">
<% @album.each do |album| %>
  <%= album.name %>
  <img src="<%= album.image_url %>" alt="skdks">
  <%= album.released_at %>
<% end %>
